struct node {
	id -> string;
	age -> int;
	bool operator < (const node & x) const {
		If age >= 60 && x.age >= 60 ?
			If age != x.age Then Return age > x.age;
			Return id < x.id;
		If age >= 60 Then Return true;
		If x.age >= 60 Then Return false;
		Return false;
	}
}a[105];
Def Main() {
	Return 0;
}
