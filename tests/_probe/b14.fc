def Fib(n -> int) -> int {
	If n <= 1 Then Return n;
	Return Fib(n - 1) + Fib(n - 2);
}
def Gcd(a -> int, b -> int) -> int {
	While b > 0 {
		t = a % b -> int;
		a = b;
		b = t;
	}
	Return a;
}
Def Main() {
	Out Fib(10), Nl;
	Out Gcd(48, 36), Nl;
	Return 0;
}
