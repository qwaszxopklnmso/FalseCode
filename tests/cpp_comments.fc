Def Main() {
	/* 同行块注释 */
	Out 1, Nl;
	x = 2 -> int; /* 行尾块注释 */
	Out x, Nl;
	/* 行首块注释 */ y = 3 -> int;
	Out y, Nl;
	z = 4 /* 表达式内注释 */ + 1 -> int;
	Out z, Nl;
	Out "/* 字符串里的不是注释 */", Nl;
	/* 跨行块注释
	被注释的内容不是代码
	*/
	a = 5 -> int; /* 开头同行
	跨行到第二行 */
	Out a, Nl;
	// // 行注释
	Out 6, Nl;
	Return 0;
}